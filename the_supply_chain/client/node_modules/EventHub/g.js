function test(repo) {
    var id = 99, user = { id: id,  ... };
    repo.saveUser(user);
    expect(repo.getUser(id)).toEqual(user);
}

function test(logger) {
    logger.log('mark rox');
    expect(logger.getLog).toEqual('mark rox');
}

var testUserRepoLogger = function(userLogger) {
    repo.saveUser(user);
}

var testLogger = new Logger() {}
testLogger.prototype = Object.create(Logger);
testLogger.log = function(msg) { console.log('I got logged: ' + msg); }

var logger = new LoggerFile('mark');
var repo = new UserRepoRedis();
var dec = new UserRepoLogger(repo, logger);
testUserLogger(dec);

